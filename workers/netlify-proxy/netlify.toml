# Netlify Edge Function 전용 사이트 — 빌드 없음, 정적 자산은 public/ 한 장.
# 엣지 함수는 netlify/edge-functions/proxy.ts 의 export const config = { path: "/" } 로 라우팅.

[build]
  publish = "public"
  # no-op — 빈 문자열이면 Netlify 가 프레임워크 자동감지(잘못된 Remix/Hydrogen) 프롬프트를 띄움.
  command = "true"
